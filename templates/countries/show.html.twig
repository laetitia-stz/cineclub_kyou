{% extends 'base.html.twig' %}

{% block title %}Countries{% endblock %}

{% block body %}
    <main class="main"> 
    <h1>Countries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Country_name</th>
                <td>{{ country.countryName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_countries_index') }}">back to list</a>

    <a href="{{ path('app_countries_edit', {'id': country.id}) }}">edit</a>

    {{ include('countries/_delete_form.html.twig') }}
    </main>
{% endblock %}
