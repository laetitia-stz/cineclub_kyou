<form method="post" action="{{ path('app_awards_delete', {'id': award.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ award.id) }}">
    <button class="btn">Delete</button>
</form>
